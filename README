Use the configuration script config.sh to deploy the symlinks to the configuration files.

$ ./config.sh deploy

Please backup and remove existing files or the symlinks will not be created. Do all editing of configuration files inside the git directory.

To stage changes for commit use:

$ ./config.sh gitadd

then commit and push.
